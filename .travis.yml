dist: trusty
sudo: required

language: go

go:
  - 1.7.x

before_install:
  ## This is a mess.  Can't get ffmpeg libraries for Trusty
#  - sudo add-apt-repository ppa:mc3man/trusty-media -y
#  - sudo apt-get -qq update
#  - sudo apt-get install -y libswscale-dev libavcodec-dev libavutil-dev libavformat-dev libavfilter-dev libavdevice-dev libavresample-dev

#  - sudo apt-get install -y libswscale-ffmpeg-dev libavcodec-ffmpeg-dev libavutil-ffmpeg-dev libavformat-ffmpeg-dev libavfilter-ffmpeg-dev libavdevice-ffmpeg-dev libswresample-ffmpeg-dev

# Don't "go get" otherwise it will try to install itself
# install:
#   - go get -x -t ./...

#script:
#  - go build example/versions.go
